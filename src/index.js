import React from 'react';
import ReactDOM from 'react-dom/client';
import CertificateApp from './serverpage.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CertificateApp />
  </React.StrictMode>
);
